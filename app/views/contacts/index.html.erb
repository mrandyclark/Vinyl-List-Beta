<style>
	body { font-family: georgia; background-color: #c0bfbb;}
	.content { width: 600px; margin: 0px auto; clear: both; text-align: center; overflow: hidden;}

	#brand  { width: 450px; margin: 0px auto;}
	#fname { float: left; width: 290px; padding-right: 5px;}
	#lname { float: right; width: 290px; padding-left: 5px;}
	
	#about { text-align: center;}
	input { font-size: 18px; line-height: 25px; height: 25px; padding: 5px; font-family: georgia;}
	#fname input, #lname input { width: 280px;}
	#email { padding-top: 15px; float: left; clear: both;}
	#email input { width: 590px;}
	
	#actions { display: none;}
	
	#counter { margin-top: 15px;}
</style>


<script type="text/javascript" src="//ajax.googleapis.com/ajax/libs/jquery/1.5.1/jquery.min.js"></script>
<script type="text/javascript">
  $(document).ready(function() {
	
	// clear an input
  	$("#fname input, #lname input, #email input").click(function() {
		var value = $(this).val();
		
		if(value == "first name" || value == "last name" || value == "email") {
			$(this).val("");
		}
	});
	
	$("#fname input").blur(function() {
		if( $(this).val() == "" ) { $(this).val("first name"); }
	});
	
	$("#email input").blur(function() {
		if( $(this).val() == "" ) { $(this).val("email"); }
	});
	
	$("#lname input").blur(function() {
		if( $(this).val() == "" ) { $(this).val("last name"); }
	});
	
  });
</script>


<div id="brand" class="content">
	<%= image_tag("brand.png")%>
</div>

<div id="about" class="content">
	<p>Vinyllist is a way for you to keep track of your vinyl collection, <Br/>see what your friends are spinning, and find new and interesting albums.</p>
	<p>Sign up below to stay tuned.</p>
</div>

<div id="form" class="content">
	<%= form_for(@contact) do |f| %>
	  <% if @contact.errors.any? %>
	    <div id="error_explanation">
	      <ul>
	      <% @contact.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

	  <div id="fname" class="field">
	    <%= f.text_field :first_name, :value => "first name" %>
	  </div>
	  <div id="lname" class="field">
	    <%= f.text_field :last_name, :value => "last name" %>
	  </div>

	  <div id="email" class="field">
	    <%= f.text_field :email, :value => "email" %>
	  </div>
	  <div id="actions">
	    <%= f.submit %>
	  </div>
	<% end %>
</div>

<div id="counter" class="content">
<p><%= pluralize(@all_contacts, "person is") %> interested in vinyllist.</p>
</div>